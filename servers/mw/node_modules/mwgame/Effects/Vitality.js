var O2 = require('o2');
var Effect = require('./Abstract/Effect.js');

O2.extendClass('ADV.Effect.Vitality', Effect, {
	__construct: function(nLevel) {
		O2.parent(this, 'Vitality', nLevel > 0 ? 'bless' : 'curse');
		this.setLevel(nLevel);
	},
	
	cast: function(ep) {
		this.getTarget().getSoul().setBonus('vitality', this.getLevel());
	},
	
	expire: function(ep) {
		this.getTarget().getSoul().setBonus('vitality', -this.getLevel());
	}
});

module.exports = ADV.Effect.Vitality;
