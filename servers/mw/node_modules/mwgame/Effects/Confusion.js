var O2 = require('o2');
var Effect = require('./Abstract/Effect.js');

// Effet de confusion
var ADV_Effect_Confusion = O2.extendClass(Effect, {
	
	__construct: function() {
		O2.parent(this, 'Confusion', 'curse');
	},
	
	cast: function(ep) {
		this.getTarget().getSoul().setBonus('confused', 1);
	},

	
	expire: function(ep) {
		this.getTarget().getSoul().setBonus('confused', -1);
	}
});

module.exports = ADV_Effect_Confusion;
