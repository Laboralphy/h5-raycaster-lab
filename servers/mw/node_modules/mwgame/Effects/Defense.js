var O2 = require('o2');
var Effect = require('./Abstract/Effect.js');

O2.extendClass('ADV.Effect.Defense', Effect, {
	__construct: function(nLevel) {
		O2.parent(this, 'Defense', nLevel > 0 ? 'bless' : 'curse');
		this.setLevel(nLevel);
	},
	
	cast: function(ep) {
		this.getTarget().getSoul().setBonus('defense', this.getLevel());
	},
	
	expire: function(ep) {
		this.getTarget().getSoul().setBonus('defense', -this.getLevel());
	}
});

module.exports = ADV.Effect.Defense;
