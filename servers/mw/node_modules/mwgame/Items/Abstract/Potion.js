var O2 = require('o2');
var Item = require('./Item.js');

/**
 * Potion de base
 */
O2.extendClass('Potion', Item, {

	type: 'potion',
	frozen: false,

	use: function(ctx) {
		if (!this.frozen) {
			this.drink(ctx);
			ctx.consume = true;
		} else {
			ctx.consume = false;
		}
	},
	
	misuse: function(ctx) {
		if (!this.frozen) {
			this.drink(ctx);
			ctx.consume = true;
		} else {
			ctx.consume = false;
		}
	}
});

module.exports = Potion;
