var O2 = require('o2');
var GCItem = require('gc').Item;

O2.extendClass('ADV.Item', GCItem, {
	
	type: '',	// type d'item
	
	action: function(ctx) {
		switch (ctx.action) {
			case 'fire':
				if (this.cool(ctx)) {
					this.fire(ctx);
				}
				break;
				
			case 'use':
				this.use(ctx);
				break;

			case 'misuse':
				this.misuse(ctx);
				break;
		}
	}
});

module.exports = ADV.Item;
